## 🤠 HOWDY 🤠

So couple of notes to get going. 

1. I would recommend downloading the iso manually. The provider seems to have issues/odd behavior when downloading an iso. 
2. Most sensitive values, I hope I got out :). I use the env() function with packer pretty heavily. See packer.env.example for the variables will accept env values. 


### When Running

